# Copy this file locally if useful. Do not commit a filled .env.
# pairing.json stays outside git and is bind-mounted at runtime.

PAIRING_FILE=/app/pairing.json
HTTP_ENDPOINT_URL=https://example.invalid/homepod-readings
POLL_INTERVAL_SECONDS=60
HOMEKIT_TIMEOUT_SECONDS=15
HTTP_TIMEOUT_SECONDS=10
