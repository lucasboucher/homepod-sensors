**Do not paste `pairing.json`, `rooms.json`, `dump.txt`, HomeKit keys, certificates, signing identities, private IPs, or HTTP endpoint URLs.**

Describe the problem with:

- macOS or Linux/Raspberry Pi version
- whether you followed `extractor/docs/OPERATIONS.md`
- `swift build` or Docker build output with secrets redacted
- collector logs that mention accessory aliases and error types only
